# Required: colon-separated user:pass:role;user2:pass2:role2
# Roles: owner | admin | member
AUTH_CREDENTIALS="admin:sekret:owner;user1:pass1:member"

# Optional: Metered REST API for dynamic session TURN credentials
# METERED_DOMAIN="yourapp.metered.live"
# METERED_API_KEY="your_metered_api_key"
#
# Fallback: static TURN credentials (used if METERED_DOMAIN/API_KEY not set)
# TURN_USERNAME="your_metered_username"
# TURN_CREDENTIAL="your_metered_credential"

# Optional: restrict CORS (default: allow all)
# CORS_ORIGIN="https://your-app.onrender.com"

# Optional: enable debug logging
# DEBUG=true
